# macOS Console Design System Workspace

Persistent design artifacts for BridgingIO macOS console are stored here.

- `MASTER.md`: global design source of truth
- `pages/*.md`: page-specific overrides

Generation and updates should be driven by the `ui-ux-pro-max` workflow and
confirmed by project owner before implementation.

