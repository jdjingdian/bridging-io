# Local Operator Interface Matrix

This document is the canonical interface matrix for trusted local operator
surfaces:

- `bridgingio-core menuconfig`
- standalone CLI management routes
- trusted local control-plane / app API
- future local UI/TUI hosts

## Interface Matrix

| Surface | Command / Interface | Callers | Input | Output | Status / Error Highlights | Apply Strategy |
| --- | --- | --- | --- | --- | --- | --- |
| Config | `core.operator_locale` (`managed-core.toml` / standalone config) | local operator, `bridgingio-core` | `en-US` or `zh-CN` | core-owned CLI/TUI locale selection | strict enum validation; unsupported locale is rejected | persisted by `menuconfig` save; runtime applies on restart |
| CLI | `bridgingio-core menuconfig [--config <path>]` | local operator, future UI tooling | optional config path | interactive TUI session, saved config path | display-safe diagnostics only, no secret plaintext display; Security page is state-gated (`uninitialized -> Init Vault`, `locked -> Unlock/Delete Vault + SSH Key Count`, `unlocked -> Import SSH Key/SSH Key Management/Create Token/Token Management/Delete Vault`); high-risk actions emit unified local authorization events with stable `flow_id`/`operation`/`phase`/`result`/`dedupe_state`; Targets `Add Target` uses mode-first flow (`storage mode -> target type -> SSH Authentication Setup -> detail editor`) and blocks sensitive create while vault is locked; plain secret-backed auth defaults `SSH Secure Access=true` but can be toggled off, sealed secret-backed auth always shows read-only `SSH Secure Access=required`; local passphrase-protected key paths are blocked in setup/edit/save with guidance to `sealed + Import Local SSH Key Into Vault`; SSH `Test Connection` uses draft-only probe semantics without implicit apply/save; root `Esc` / `< Exit >` save confirm is shown only when current draft differs from session baseline, and reverted fields clear dirty state; `Apply Target --->` commits only the target edit session and does not imply file persistence | `Save` persists config and returns restart hint when changed; `Apply Target --->` is in-session commit only |
| CLI | `menuconfig -> Core Settings -> Cache Settings` | local operator | `storage.artifacts.backend`, `storage.artifacts.max_bytes` | cache storage mode + capacity editor | operator-facing display labels are localized (`Memory`/`Filesystem`, `运行内存`/`文件系统`), but persisted config values remain canonical (`memory`/`filesystem`) | saved on `Save`/`Apply` |
| CLI | `menuconfig -> Core Settings -> HTTP Interface Settings` | local operator | `model_plane.http.host`, `model_plane.http.port`, `model_plane.http.allow_non_loopback` | HTTP listening and exposure editor | `allow_non_loopback` remains explicit and stateful in submenu (not hidden fallback); non-loopback exposure still requires explicit enable + auth validation in core | saved on `Save`/`Apply` |
| Runtime Logs | `runtime_root/logs/local-authorization.jsonl` + `runtime_root/logs/menuconfig-session.jsonl` | local operator, support/debug tooling | display-safe event payloads only | append-only JSONL events (`vault.unlock`, `vault.delete`, `ssh_key.import`, `ssh_key.delete`, `auth.token.create`, `auth.token.delete`) + session breadcrumbs (including `ssh.test_connection` flow nodes) | `INFO` persists key authorization/session milestones; `DEBUG` breadcrumb persistence requires `core.log_level=debug|trace`; SSH test flow logs stable `flow_id` with started / finished / failed / cancelled / timed-out phases only; sink failures are best-effort and must not block main flow | N/A |
| CLI | `bridgingio-core --help` / `bridgingio-core help` | local operator, docs tooling | optional `--config <path>` for locale source | localized help with grouped sections (`Usage` / `Commands` / `Options` / `Notes`) | locale falls back to `en-US`; non-TTY output remains plain text without ANSI escapes | none |
| CLI | `bridgingio-core --version` / `-V` | local operator, automation | none | core version string from workspace Cargo truth | format validated as `YYMM.DD.BuildNumber` with Cargo semver constraints | none |
| CLI | `bridgingio-core run [--config <path>]` | local operator | optional config path | foreground runtime process | startup lifecycle errors use shared error/status contract | N/A |
| CLI | `bridgingio-core -d [--config <path>]` | local operator, launcher | optional config path, one-shot startup carrier if needed | detached runtime process | detached mode overrides vault trigger to `on-core-start`; startup remains fail-closed | N/A |
| CLI | `bridgingio-core vault init` | local operator | config path | display-safe initialization summary | validation/dependency errors are structured and display-safe | live action |
| CLI | `bridgingio-core vault delete` | local operator | config path | display-safe delete summary (`lock_state=uninitialized`) | requires local-admin attestation + confirmation; writes unified authorization event schema to runtime `logs/local-authorization.jsonl`; only deletes current runtime vault store; does not delete shared os-native keyring entries | live action |
| CLI | `bridgingio-core vault import` | local operator | config path, secret input route | display-safe import summary | secret input conflicts fail closed; no plaintext argv | live action |
| CLI | `menuconfig -> Security -> Import SSH Key` | local operator | key name, label, source path, hidden passphrase (optional) | display-safe import summary (`credential_ref`, `status`, `record_id`) | encrypted OpenSSH key requires trusted local passphrase capture; passphrase never appears in menu rows/status/logs; duplicate key name is rejected and requires delete-first workflow; action emits `operation=ssh_key.import` events with stable `flow_id` | live action |
| CLI | `menuconfig -> Security -> SSH Key Management` | local operator | none | display-safe list/detail + `Delete SSH Key` | unlocked-only list/detail; locked state exposes only aggregate count; detail never reveals key material; deleting a key clears bound target `credential_ref` values and emits `operation=ssh_key.delete` | live action |
| CLI | `bridgingio-core vault unlock` | local operator | config path, allowed local carrier | display-safe unlock summary | locked / verification / unavailable paths remain structured; verified `os-native` path applies in-process singleflight (`leader`/`joined`/`cancelled`/`failed`) so near-simultaneous unlock flows share one provider verification | live action |
| CLI | `bridgingio-core auth token create` | local operator | config path, label, optional expiry | one-time reveal result | attestation/verification errors remain structured; expiring token create is blocked when rollback clock anomaly is detected; emits `operation=auth.token.create` in unified authorization schema | live action |
| CLI | `bridgingio-core auth token revoke` | local operator | config path, token id | display-safe revoke summary | not-found / validation / dependency errors remain structured | live action |
| CLI | `bridgingio-core auth token delete` | local operator | config path, token id | display-safe delete summary (`status=deleted`) | requires local-admin attestation + confirmation; only `revoked` token can be deleted (`expired` must be revoked first); emits `operation=auth.token.delete` with shared `flow` semantics | live action |
| Control Plane | `attach_ui` | trusted local UI host | host id, ui session id, ui kind | attached response, readiness state | ownership conflict and not-ready are structured | `live_applied` |
| Control Plane | `probe_host_instance` | trusted local UI/TUI host | none | ownership/runtime summary | startup / recovery state is structured | none |
| Control Plane | `get_settings` | trusted local UI/TUI host | none | `CoreSettingsView` | display-safe settings snapshot | none |
| Control Plane | `update_settings` | trusted local UI/TUI host | validated settings delta | accepted + `apply_strategy` | validation/not-ready/method-not-implemented are structured | `live_applied` or `restart_required` |
| Control Plane | `get_vault_state` | trusted local UI/TUI host | none | `VaultStateProjectionView` | display-safe lock/protector summary | none |
| Control Plane | `unlock_vault` | trusted local UI/TUI host | attestation + allowed method | `vault_unlocked` response | locked/verification/mismatch remain structured; successful unlock reconciles sensitive target public cache from vault-authoritative `target-profile` payloads | live action |
| Control Plane | `list_profiles` / `get_profile` | trusted local UI/TUI host | optional target id | projected target catalog/profile | sensitive target projection states are formalized as `locked` / `resolved` / `repair-needed` / `tamper`; locked view returns public cache only | none |
| Control Plane | `create_local_admin_intent` / `complete_local_admin_attestation` | trusted local UI/TUI host | action context | intent / attestation records | verification and mismatch errors remain structured | live action |
| Control Plane | `init_vault` / `delete_vault` | trusted local UI/TUI host | current runtime vault context + attestation for delete | `vault_initialized` / `vault_deleted` response | delete requires attestation + confirmation; delete returns `uninitialized` and removes token management entry points | live action |
| Control Plane | `create_agent_token` / `list_agent_tokens` / `revoke_agent_token` / `delete_agent_token` / `update_agent_token_label` / `update_agent_token_access` | trusted local UI/TUI host | display-safe token management input + attestation for create/delete | one-time reveal or display-safe summaries | `Token Management` uses list -> detail topology: list row exposes `(stable-serial) label [expiry] [status] --->`; detail includes label edit, access toggle, display-safe fingerprint, expiry/status, `Permissions Management --->` placeholder and status-gated revoke/delete. Delete is hidden until `revoked`; runtime still enforces `delete requires revoked`. | live action |
| Control Plane | `SSH Authentication Setup` (`none` / `password` / `private-key`, key source local/vault, secure-access toggle/state) | trusted local UI/TUI host | target id + auth kind/source edits + optional imported credential selection | typed `targets[*].ssh_auth` is updated; private-key vault selection binds canonical `vault://.../ssh-private-key/<name>` locator; legacy `credential_ref` remains synchronized for compatibility | auth kind rows are exclusive single-choice picks and use `Space` to switch (`Enter` enters selected kind details); plain password selection requires explicit risk confirmation; plain `private-key` exposes local path only (no vault-source row); `Continue To Detail Editor` remains disabled until required auth inputs are valid (password non-empty, local key path readable and non-passphrase, or vault key selected for sealed); sensitive+ssh flow is unlocked-only; locked sensitive target must not expose bound key identity; local passphrase-protected key path is blocked with guidance to unlock vault and use `Import Local SSH Key Into Vault`; sensitive secret-backed auth cannot disable secure access | live action |
| CLI | `menuconfig -> Targets -> SSH target -> Test Connection` | local operator | current SSH draft fields (`host`/`port`/`username`/`ssh_auth` etc.) + `timeout_ms` (default `2000`) | concise result popup (`Connection succeeded` / `Connection failed` / `Connection cancelled` / `Connection timed out`) | plain SSH entry is always visible in `Connection Profile`; sealed SSH entry is visible only in unlocked `Sensitive Overlay`; auth entry is `SSH Authentication --->`; plain + vault-backed credential may return controlled preflight failure while vault is locked; broker endpoint is injected only after runtime bind/listen ready and failure is classified as broker-specific; direct identity path injects explicit `-i`/`IdentityFile` + `IdentitiesOnly=yes` + `IdentityAgent=none`, and failures must not be reported as broker unavailable; no implicit unlock; detailed diagnostics remain display-safe session logs only | live action |
| Model Plane HTTP | `POST /tool/terminal.exec` bearer token authn | MCP clients, local automation | bearer token + target/tool context | execution result or structured authn rejection | bearer token authn rejection uses `domain=authn`, `common_code=credential_rejected`, module subcodes `agent_token_invalid` / `agent_token_disabled` / `agent_token_revoked` / `agent_token_expired`, plus display-safe message and recovery hint | live action |

## Maintenance Rules

- Every new trusted local command, event, or settings mutation path must update
  this matrix.
- `core.operator_locale` is a core-owned operator surface setting only. Future
  UI implementations must not treat it as UI locale source of truth.
- Core version truth is `source/rust/Cargo.toml` `[workspace.package].version`.
  CLI/version metadata must stay aligned with it; do not add duplicate version
  constants in code.
- `bridgingio-core menuconfig` visual grammar, focusability, popup behavior,
  key semantics, and viewport rules must stay aligned with
  `docs/matrix/MENUCONFIG_STYLE_MATRIX.md`; feature changes that alter these
  behaviors must update that matrix together with the relevant OpenSpec spec.
- `method_not_implemented`, `not_ready`, `restart_required`, and equivalent
  formal states must be captured here instead of only in prose docs.
- This matrix must stay aligned with the shared error/status contract and the
  self-test / contract matrix.
