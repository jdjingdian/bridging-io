{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability for the BridgingIO Tauri shell host.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
