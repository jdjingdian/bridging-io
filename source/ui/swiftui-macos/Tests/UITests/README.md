# macOS UI Test Plan

UI tests should validate these baseline flows:

1. select target and open session
2. show session capability summary
3. display command timeline card
4. open artifact detail and refine view
5. approve or deny pending sensitive operation

This file is the initial contract; concrete XCTest cases will be added alongside
UI implementation.

