## 1. 选中态渲染实现

- [x] 1.1 在 menu 渲染层为菜单项新增 `selected` 反色样式映射，并复用按钮区已存在的反色 token/样式入口
- [x] 1.2 建立菜单项状态优先级（`disabled` > `selected` > `normal`），确保禁用项不会被渲染为可执行高亮态
- [x] 1.3 校对菜单区与按钮区焦点切换逻辑，确保两个区域在选中态下遵循一致视觉策略

## 2. 终端兼容与降级

- [x] 2.1 接入或复用终端能力探测结果，识别反色不可用环境
- [x] 2.2 为反色不可用环境实现可见 fallback 样式（与未选中项可明确区分）
- [x] 2.3 补充配置或常量说明，明确默认策略与 fallback-only 应急切换方式

## 3. 测试与回归验证

- [x] 3.1 新增键盘导航测试，验证焦点从选项 A 移动到选项 B 时反色高亮同步切换
- [x] 3.2 新增菜单区与按钮区焦点往返测试，验证两区选中态视觉一致性
- [x] 3.3 新增反色不可用场景测试，验证 fallback 可见且不会静默回退
- [x] 3.4 新增禁用项场景测试，验证禁用语义优先级不被选中态覆盖
