## 1. 导航逻辑改造

- [x] 1.1 定位 menuconfig 列表 `↑/↓` 焦点更新路径，确认当前边界 clamp 的实现位置。
- [x] 1.2 将首尾边界处理改为循环计算（首项 `↑` 跳末项、末项 `↓` 跳首项），并确保仅作用于菜单列表垂直导航。
- [x] 1.3 为空列表与单项列表增加防护分支，保证按键后状态稳定且不出现越界/异常。

## 2. 一致性与回归测试

- [x] 2.1 新增或更新单元测试：验证第一项按 `↑` 会跳转到最后一项。
- [x] 2.2 新增或更新单元测试：验证最后一项按 `↓` 会跳转到第一项。
- [x] 2.3 新增或更新边界测试：空列表与单项列表下按 `↑/↓` 不报错且状态可预测。

## 3. 验证与文档同步

- [x] 3.1 运行 menuconfig 相关测试集并记录结果，确认未引入按钮栏或其他非列表导航回归。
- [x] 3.2 在交互说明/帮助文案中补充“列表支持首尾循环导航”的行为说明（如已有对应文档入口）。
- [x] 3.3 完成手工回归：主菜单与至少一个子菜单验证循环导航行为一致。
